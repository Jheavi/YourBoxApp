import {
  AUTH0_DOMAIN,
  AUTH0_CLIENTID,
  AUTH0_REDIRECTURL,
  AUTH0_LOGOUTURL
} from '@env'

const auth0data = {
  domain: AUTH0_DOMAIN,
  clientId: AUTH0_CLIENTID,
  redirectUri: AUTH0_REDIRECTURL,
  logoutUri: AUTH0_LOGOUTURL
}

export default auth0data
